# Using Built-in Modules
# The 'sys' and 'os' modules are included in every Python installation.
import sys
import os

print("Python version:", sys.version.split()[0])
print("Current Directory:", os.getcwd())
