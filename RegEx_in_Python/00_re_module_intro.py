import re
# The 're' module is used for regular expressions.
# Raw strings (r"...") are used so backslashes are not treated as escape characters.
pattern = r"\n"
print("re module loaded. Pattern created:", pattern)
